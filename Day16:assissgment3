/*Write a c program to print factorial of n*/
PROGRAM-
#include<stdio.h>
int main()
{
    int i,num,fact=1;
    printf("enter a number:");
    scanf("%d",&num);
    for(i=1;i<=num;i++)
    fact=fact*i;
    printf("factorial of %d is:%d\n",num,fact);
    return 0;
}
